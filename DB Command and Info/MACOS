If you use mySql on mac and attempt to insert/delete an existing table, the application will automatically crash
It has not crashed in sub tables yet
MAC USERS USE VERSION 28! You can then click and edit tables from there without injection commands. It is as easy as clicking and editing. 